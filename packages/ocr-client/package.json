{
  "name": "@midas/ocr-client",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable OCR client for the Midas Expense Engine: talks to services/ocr-engine, plus shared preprocessing and rule-based field inference used by every embedder.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.5"
  }
}
