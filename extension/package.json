{
  "name": "@midas/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/manifest.json && cp -R icons dist/icons",
    "build:firefox": "vite build --mode firefox",
    "package": "npm run build && mkdir -p ../apps/web/public && rm -f ../apps/web/public/midas-extension.zip && cd dist && zip -r ../../apps/web/public/midas-extension.zip .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.285",
    "@types/jsdom": "^30.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
