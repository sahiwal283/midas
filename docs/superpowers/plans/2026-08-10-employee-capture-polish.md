# Employee Capture Polish — Implementation Plan

> **For agentic workers:** Execute task-by-task. Spec: `docs/superpowers/specs/2026-08-10-employee-capture-polish-design.md`

**Goal:** Brand + UX polish for employee capture (Phase 6). Version `0.25.0-alpha`. No schema migrations.

## Tasks

1. **StatusBadge** — user labels `Approved ✓` / `Accounting complete ✓`
2. **ExpenseDetail** — pass `zohoExpenseId` to user StatusBadge; Company wording
3. **Dashboard** — action-first subtitle/lanes; status with checkmarks
4. **ExpenseNew** — cream/ink/gold + step chrome (Choose → Details → Done)
5. **UploadRetryBanner** — brand attention tokens
6. **Ship** — bump versions, CHANGELOG, deploy CT 3120, verify `/meta`
